{% extends "templates/admin/base.html.twig" %}

{% block title %}Confirmar Exclusão de Educador{% endblock %}

{% block content %}
    <main class="flex-1 p-6">
        <h2 class="text-2xl font-bold text-red-700 mb-6">Confirmar Exclusão de Educador</h2>

        {% if educador %}
            <div class="bg-white shadow-md rounded-lg p-6 max-w-lg mx-auto text-center">
                <p class="text-lg text-gray-700 mb-4">Você tem certeza que deseja excluir o educador:</p>
                <p class="text-xl font-bold text-red-600 mb-6">{{ educador.perfil.nome }} {{ educador.perfil.sobrenome }} ({{ educador.usuario.email }})?</p>
                <p class="text-gray-600 mb-8">Esta ação não pode ser desfeita.</p>

                <div class="flex justify-center space-x-4">
                    <a href="{{ base_url }}/admin/educadores" class="px-6 py-2 bg-gray-200 text-gray-800 font-medium rounded-md hover:bg-gray-300">
                        Cancelar
                    </a>
                    <form action="{{ base_url }}/admin/educadores/delete/{{ educador.usuario.id }}" method="POST">
                        <button type="submit" class="px-6 py-2 bg-red-600 text-white font-medium rounded-md hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-red-300">
                            Excluir Permanentemente
                        </button>
                    </form>
                </div>
            </div>
        {% else %}
            <div class="bg-white shadow-md rounded-lg p-6 max-w-lg mx-auto text-center">
                <p class="text-lg text-gray-700">Educador não encontrado para exclusão.</p>
                <a href="{{ base_url }}/admin/educadores" class="mt-4 inline-block px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700">Voltar para a lista</a>
            </div>
        {% endif %}
    </main>
{% endblock %}