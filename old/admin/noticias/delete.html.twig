{% extends "templates/admin/base.html.twig" %}

{% block title %}Confirmar Exclusão{% endblock %}

{% block content %}
<h2 class="text-2xl font-bold text-gray-800 mb-6">Confirmar Exclusão</h2>

<div class="bg-white shadow-md rounded-lg p-6 font-['Poppins']">
    <p class="text-lg text-gray-800">
        Você tem certeza que deseja deletar a notícia: <strong>"{{ noticia.getTitulo() }}"</strong>?
    </p>
    <p class="text-gray-600 mt-2">Esta ação não poderá ser desfeita.</p>

    <form action="{{ base_url }}/admin/noticias/delete/{{ noticia.getId() }}" method="POST" class="mt-6">
        <div class="flex justify-end">
            <a href="{{ base_url }}/admin/noticias" class="mr-3 px-4 py-2 bg-gray-200 text-gray-800 text-base font-medium rounded-md hover:bg-gray-300 focus:outline-none focus:ring-2 focus:ring-gray-300">
                Cancelar
            </a>
            <button type="submit" class="px-4 py-2 bg-red-600 text-white text-base font-medium rounded-md hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-red-300">
                Sim, Deletar
            </button>
        </div>
    </form>
</div>
{% endblock %}